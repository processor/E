Box struct { 
  width  : f64
  height : f64
  length : f64 

  from (width: f64, height: f64, length: f64) => Box { width, height, length }
}

Geometry impl for Box { 
  faces { 

  }

  vertices {
   
  }
}